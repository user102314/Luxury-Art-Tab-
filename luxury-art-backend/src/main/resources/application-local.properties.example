# Copier vers application-local.properties (non versionné) et renseigner le mot de passe.
# Profil local activé par défaut via spring.profiles.default=local

spring.datasource.password=VOTRE_MOT_DE_PASSE

# Colissimo API — delivery.colissimo.com.tn > Profil > Identifiants API
colissimo.enabled=true
colissimo.utilisateur=clt_liv_XXXX
colissimo.pass=VOTRE_CLE_API
